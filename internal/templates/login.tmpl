{{define "login"}}
    <!DOCTYPE html>
    <html lang="en">
    {{ template "header" }}

    <body>
        {{template "navbar"}}
    <div class="container">
        <div class="row">
            <div class="col-md">
                <h1 class="form-heading">Log in</h1>
                <form action="/login" method="post" enctype="application/x-www-form-urlencoded">
                    <div class="form-group">
                        <input name="email" type="email" class="short" placeholder="Email">
                    </div>
                    <div class="form-group">
                        <input name="password" type="password" class="short" placeholder="Password">
                    </div>
                    <button type="submit" class="submit">Submit</button>
                </form>
            </div>
            <div class="col-md">
                {{/*col2*/}}
            </div>
            <div class="col-md">
                {{/*col3*/}}
            </div>
            {{/*<div class="col-6"></div>*/}}
        </div>

    </div>



    </body>
    </html>
{{end}}
